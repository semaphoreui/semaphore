version: "3.4"

services:
  dredd:
    environment:
      SEMAPHORE_DB_DIALECT: postgres
      SEMAPHORE_DB_CONFIG: '{"host": "db:5432","user": "forge","pass": "forge","name": "forge","options": {"sslmode": "disable"}}'
    depends_on:
      - server
      - db
